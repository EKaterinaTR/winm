# Consumer-only env (optional). Used when running consumer with its own config.
# Copy to .env or set in deploy; consumer needs EXPORT_DIR for JSONL export.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password
RABBITMQ_URL=amqp://guest:guest@localhost:5672/
EXPORT_DIR=./exports
